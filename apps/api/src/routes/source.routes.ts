// Phase 1 — source connection routes (Notion OAuth callback, list sources, delete source).
// TODO: implement when instructed.
import { Router } from 'express';

export const sourceRouter = Router();
