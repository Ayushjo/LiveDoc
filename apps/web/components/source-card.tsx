// Phase 1 — displays a connected source with sync status badge and actions.
// TODO: implement when instructed.
import type { Source } from '@livedoc/types';

interface SourceCardProps {
  source: Source;
}

export function SourceCard({ source: _source }: SourceCardProps) {
  return <div />;
}
